/*------底圖*/	
$(document).ready(function(){
	
   $('.bigbanner').slick({
  dots: true,
  infinite: true, //無限輪播
  speed: 500,
  fade: true,
  autoplay:true, //自動播放 
  cssEase: 'linear'
  });
  //
  
  $('.cp_photo').slick({
  dots: true,
  infinite: true,
  speed: 500,
  fade: true,
  autoplay:true, //自動播放
  cssEase: 'linear'
});
	
	
  
});